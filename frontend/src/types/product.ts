import { TriggerConfig } from "react-hook-form"

export type Product = {
    name: string, 
    age: number
}
export type ProductType ={
    name: String,
    code: string,
    images: string,
    price: number,
    description: string,
    slug: string,
    subcategory_id: any,
    quanlity: number,
    injectionPark_id: any,
    available: boolean,
    start_use: Date,
    end_use: Date,


    



}